const calculateTotal = (unitPrice, quantity) => {
    // Calculate the total cost
    return unitPrice * quantity;
};



// Call the function and assign the result to totalCost
const totalCost = calculateTotal(15, 3);

// Log the total cost
console.log(totalCost);

